PACKAGE 972 - the last of the client-side branding
================================================================================

Twenty files. Complete replacements. Mind the two subfolders.

  pages/admin.js
  pages/application-calendar.js
  pages/applications.js                 <- INCLUDES 971's change to this file
  pages/business-financials.js
  pages/business-financials/cash-schedule.js
  pages/business-financials/cashflow.js
  pages/business-financials/invoice-finance.js
  pages/business-financials/margin.js
  pages/business-financials/monthly.js
  pages/business-financials/retentions-due.js
  pages/business-financials/vat-refund.js
  pages/commercial-email-templates.js
  pages/commercial-scorecard.js
  pages/lessons-learnt.js
  pages/negotiating.js
  pages/operations/project-financials.js
  pages/procurement-savings.js
  pages/sales-crm.js
  pages/scorecard.js
  pages/scorecard-crm.js

Nineteen tab titles, plus three pieces of text people actually read:

  commercial-email-templates   "Auto-CC Rock Roofing CM"
  scorecard-crm                "To at least one address outside Rock Roofing"
  applications                 the APPLICATION EMAIL SIGNATURE - it signed off
                               "Rock Roofing Ltd" under the sender's name, on
                               a document sent to a client. Also the recipient
                               picker's group heading.


THE BUNDLE CHECK EARNED ITS KEEP AGAIN
================================================================================
I generated these edits from a table, and the relative import depth for the
business-financials and operations subfolders was written as

    '.../components/TenantProvider'

Three dots. Not a path. Eight files.

esbuild compiled every one of them CLEANLY - a single-file compile does not
resolve imports. Only bundling caught it, and it would have failed the Vercel
build exactly the way 965 did.


WHAT IS NOW LEFT, AND IT IS EIGHT LINES IN FIVE FILES
================================================================================
After this the client-side sweep is finished except for:

  VariationBuilder x2    "Rock Roofing Limited" on a contractual document and
                         "Rock Roofing" above the signature block. Needs
                         legalName decided, not a relabel.
  operations/forms.js    the two print-window HTML strings. Different shape -
  operations/projects.js they need the logo URL, not the component.
  outstanding-invoices   '[Rock Roofing QS Name]' - LEAVE IT. A legacy merge
                         token kept so customers' saved templates keep
                         substituting. Renaming it silently breaks the
                         templates someone bothered to customise.
  platform/index.js      an example sender address on a page that 404s.
  design/oms.js          a comment naming a hex colour.

Unrelated, noticed on the way past: components/ContactPicker.js line 106 sets
`width` twice in one object literal. esbuild's duplicate-key warning. The
later one wins, so the first is dead - the same shape of fault that was
zeroing a retention column two days ago. Worth a look, not in this package.


CHECKED
================================================================================
- Full repo sweep: eight visible "Rock Roofing" left client-side, all five
  files listed above, all deliberate.
- Every `brand` declared in the component that uses it, corrected scanner.
- All twenty compile AND bundle.


SMOKE TEST
================================================================================
1. Demo: tab titles across Scorecards, Lessons Learnt, Negotiating,
   Procurement Savings, Admin, and the Business Financials pages.
2. Demo applications: open the send modal and read the signature block. It
   should sign off with the tenant name, not Rock Roofing Ltd.
3. Rock: unchanged everywhere, and the same signature block still correct.
