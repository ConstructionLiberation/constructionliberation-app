PACKAGE 1005 - "Certs" becomes "Testing"
================================================================================

Six files. Complete replacements. No re-seed.

  lib/designShell.js
  pages/design/leak-test-certs.js
  pages/design/[project]/leak-test-certs.js
  pages/design/[project]/oms.js
  pages/api/design-oms.js               <- SUPERSEDES 1003 AND 1004
  pages/api/design-leak-test-certs.js   <- supersedes 1003

If 1003 and 1004 are not yet deployed, take the copies from THIS package for
those two files - it carries their changes as well as this one.


WHAT CHANGED
================================================================================
The same places 1003 touched, one word further:

  Design nav                 Testing
  page heading and tab       Testing
  upload button              + Upload Testing
  empty state                No Testing Yet
  the O&M page's list of what goes into the manual
  the O&M PDF section heading

Key and slug still leak-test-certs, for the same reason as before: they are
the route, the stored category and the API. Nothing filed already moves.


TWO JUDGEMENT CALLS, BOTH EASY TO REVERSE
================================================================================
The O&M manual's SECTION HEADING was "Leak Test Certificates", which 1003
shortened to "Certificates". It now reads "Testing". That is the one that
reaches a customer at handover, so if you would rather it stayed
"Certificates" in the formal document while the nav says Testing, say so -
it is one line.

The FALLBACK NAME for a file uploaded without one was "Leak Test Cert", then
"Cert". It is now "Test", because "Testing" reads oddly as the name of a
single document.


SMOKE TEST
================================================================================
1. Design nav reads Testing.
2. Open it - heading, tab title, upload button, empty state.
3. Build an O&M on a project with testing documents: the section reads
   Testing.
4. Existing links to /design/<project>/leak-test-certs still work, and
   documents already uploaded are still there.
